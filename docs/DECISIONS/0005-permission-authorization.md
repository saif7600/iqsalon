# ADR 0005: Permission-based authorization

Accepted. Roles are assignable bundles; API behavior checks explicit permissions. This prevents role-name conditionals from spreading through business logic.
